# Базовый образ
FROM node:14

# Создаем директорию приложения
WORKDIR /usr/src/app

# Копируем файлы зависимостей
COPY package*.json ./

# Устанавливаем зависимости
RUN npm install

# Копируем остальные файлы проекта
COPY . .

# Собираем приложение, если требуется
# Удалите эту строку, если ваше приложение не требует сборки
# или убедитесь, что у вас есть соответствующий скрипт в package.json
RUN npm run build

# Запуск
CMD ["node", "server/index.js"]